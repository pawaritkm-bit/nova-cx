-- =====================================================================
-- 0093 — เฟส 9b กลุ่ม BC/ข้อ3 (docs/06-accounting-features-roadmap.md, หมวด 0.5) — แยก "รอบจ่าย" ออกจาก
--   "หน่วยยื่นภาษี/ประกันสังคมรายเดือน" — คอลัมน์แรกของสถาปัตยกรรมนี้: flag ต่อลูกค้าว่าจ่ายเงินเดือนความถี่ไหน
--
--   ★ ค่า default 'monthly' — ลูกค้าเดิมทุกรายที่มีอยู่ก่อนเฟสนี้ได้ค่านี้อัตโนมัติ (ไม่มีใครได้รับพฤติกรรมใหม่
--     โดยไม่ได้ตั้งใจ) — lib/accounting/payroll.ts::createDraftRun ปฏิเสธสร้างรอบซ้ำเดือน/ปีเดียวกันเหมือนเดิม
--     ทุกประการเมื่อ pay_frequency='monthly' (ที่ชั้นแอปพลิเคชัน — ดู 0095/0096 ที่เอา unique เดิมออกจาก DB)
--   ★ เปิดสร้างหลายรอบ/เดือนได้เฉพาะลูกค้าที่นักบัญชีตั้งค่าเป็น 'non_monthly' เองเท่านั้น (เช่น จ่ายรายสัปดาห์/
--     รายปักษ์) — ตั้งค่าผ่านแท็บ "ตั้งค่าบัญชี" ของ payroll-employees (T140)
--
-- (หมายเหตุเลข migration: เอกสารแผนเดิมเรียกไฟล์นี้ว่า 0092 — เลื่อนเป็น 0093 เพราะ 0092 ถูกใช้ไปแล้วกับ
--  prior_employer_ytd_* ของกลุ่ม BD ที่ implement มาก่อนหน้านี้ในสาขาอื่น)
-- =====================================================================

alter table public.payroll_settings
  add column if not exists pay_frequency text not null default 'monthly'
    check (pay_frequency in ('monthly', 'non_monthly'));

notify pgrst, 'reload schema';
