-- =====================================================================
-- 0106 — wishlist ข้อ 6 (ส่งสลิปเงินเดือนเป็นชุดทางอีเมล) — เพิ่มคอลัมน์ email ให้พนักงาน
--   ใช้เป็นปลายทางส่ง PDF สลิปเงินเดือนของแต่ละคนตอนกดส่งจากหน้ารอบเงินเดือน
--
-- non-destructive: ALTER เพิ่มคอลัมน์ nullable — พนักงานเดิมทุกคนเป็น null (ไม่กระทบอะไรจนกว่าจะกรอกเอง)
--   พนักงานที่ไม่มี email จะถูกข้าม (skip) ตอนส่งชุด ไม่ error ทั้งรอบ
-- =====================================================================

alter table public.payroll_employees
  add column if not exists email text;

notify pgrst, 'reload schema';
